# System Architecture & Data Flow

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Indeed Job Scraper System                     │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   User       │      │  Config      │      │  .env        │
│   Input      │─────▶│  (config.py) │      │  (secrets)   │
└──────────────┘      └──────────────┘      └──────────────┘
                              │                     │
                              │                     │
                              ▼                     ▼
                      ┌──────────────────────────────┐
                      │   IndeedScraper Class        │
                      │  (indeed_scraper.py)         │
                      │                              │
                      │  • ProxyRotator              │
                      │  • URL Builder               │
                      │  • HTTP Client               │
                      │  • HTML Parser               │
                      │  • Data Extractor            │
                      └──────────────────────────────┘
                              │
                              │
                ┌─────────────┴──────────────┐
                │                            │
                ▼                            ▼
        ┌──────────────┐           ┌──────────────┐
        │  proxies.txt │           │  Indeed.com  │
        │  (10 proxies)│           │  (target)    │
        └──────────────┘           └──────────────┘
                                           │
                                           │ HTML Response
                                           ▼
                                   ┌──────────────┐
                                   │ BeautifulSoup│
                                   │   Parser     │
                                   └──────────────┘
                                           │
                                           │ Structured Data
                                           ▼
                                   ┌──────────────┐
                                   │  Supabase    │
                                   │  PostgreSQL  │
                                   │  Database    │
                                   └──────────────┘
                                           │
                                           ▼
                                   ┌──────────────┐
                                   │ indeed_jobs  │
                                   │    table     │
                                   └──────────────┘
```

## 🔄 Data Flow Diagram

```
START
  │
  ▼
┌─────────────────────────┐
│ Load Configuration      │
│ • Keywords              │
│ • Locations             │
│ • Pages, Delays         │
└─────────────────────────┘
  │
  ▼
┌─────────────────────────┐
│ Initialize Components   │
│ • Load Proxies          │
│ • Connect to Supabase   │
│ • Load Existing Links   │
└─────────────────────────┘
  │
  ▼
┌─────────────────────────┐
│ For Each Keyword        │◀─────┐
└─────────────────────────┘      │
  │                              │
  ▼                              │
┌─────────────────────────┐      │
│ For Each Location       │◀──┐  │
└─────────────────────────┘   │  │
  │                           │  │
  ▼                           │  │
┌─────────────────────────┐   │  │
│ For Each Page           │◀┐ │  │
└─────────────────────────┘ │ │  │
  │                         │ │  │
  ▼                         │ │  │
┌─────────────────────────┐ │ │  │
│ Build Search URL        │ │ │  │
└─────────────────────────┘ │ │  │
  │                         │ │  │
  ▼                         │ │  │
┌─────────────────────────┐ │ │  │
│ Select Random Proxy     │ │ │  │
└─────────────────────────┘ │ │  │
  │                         │ │  │
  ▼                         │ │  │
┌─────────────────────────┐ │ │  │
│ Make HTTP Request       │ │ │  │
│ • Random User Agent     │ │ │  │
│ • Retry on Failure      │ │ │  │
└─────────────────────────┘ │ │  │
  │                         │ │  │
  ▼                         │ │  │
┌─────────────────────────┐ │ │  │
│ Parse HTML              │ │ │  │
│ • Find Job Cards        │ │ │  │
│ • Extract Data          │ │ │  │
└─────────────────────────┘ │ │  │
  │                         │ │  │
  ▼                         │ │  │
┌─────────────────────────┐ │ │  │
│ For Each Job            │ │ │  │
│ • Check Duplicate       │ │ │  │
│ • Validate Data         │ │ │  │
│ • Save to Supabase      │ │ │  │
└─────────────────────────┘ │ │  │
  │                         │ │  │
  ▼                         │ │  │
┌─────────────────────────┐ │ │  │
│ Random Delay            │ │ │  │
│ (3-8 seconds)           │ │ │  │
└─────────────────────────┘ │ │  │
  │                         │ │  │
  ▼                         │ │  │
┌─────────────────────────┐ │ │  │
│ More Pages?             │─┘ │  │
└─────────────────────────┘   │  │
  │ No                        │  │
  ▼                           │  │
┌─────────────────────────┐   │  │
│ More Locations?         │───┘  │
└─────────────────────────┘      │
  │ No                           │
  ▼                              │
┌─────────────────────────┐      │
│ More Keywords?          │──────┘
└─────────────────────────┘
  │ No
  ▼
┌─────────────────────────┐
│ Generate Summary        │
│ • Total Jobs Scraped    │
│ • New Jobs Added        │
│ • Duplicates Skipped    │
└─────────────────────────┘
  │
  ▼
END
```

## 🏗️ Component Breakdown

### 1. Entry Points
```
┌──────────────────────────────────────┐
│        Entry Point Scripts           │
├──────────────────────────────────────┤
│                                      │
│  run_scraper.py                      │
│  ├─ Loads config.py                  │
│  ├─ Initializes IndeedScraper        │
│  └─ Calls scrape_jobs()              │
│                                      │
│  test_setup.py                       │
│  ├─ Validates dependencies           │
│  ├─ Tests connections                │
│  └─ Verifies configuration           │
│                                      │
│  view_jobs.py                        │
│  ├─ Queries Supabase                 │
│  ├─ Displays results                 │
│  └─ Exports data                     │
│                                      │
└──────────────────────────────────────┘
```

### 2. Core Classes
```
┌──────────────────────────────────────┐
│        ProxyRotator Class            │
├──────────────────────────────────────┤
│  • load_proxies()                    │
│  • get_random_proxy()                │
│  • get_next_proxy()                  │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│       IndeedScraper Class            │
├──────────────────────────────────────┤
│  • __init__()                        │
│  • load_existing_links()             │
│  • get_headers()                     │
│  • build_search_url()                │
│  • make_request()                    │
│  • parse_job_card()                  │
│  • scrape_search_page()              │
│  • save_to_supabase()                │
│  • scrape_jobs()                     │
└──────────────────────────────────────┘
```

### 3. Data Pipeline
```
Indeed.com
    │
    │ (HTTP GET with proxy)
    ▼
HTML Response
    │
    │ (BeautifulSoup parsing)
    ▼
Job Cards
    │
    │ (Data extraction)
    ▼
Job Objects
    │ {
    │   title: "...",
    │   company: "...",
    │   location: "...",
    │   salary: "...",
    │   summary: "...",
    │   link: "...",
    │   posted_date: "..."
    │ }
    │
    │ (Duplicate check)
    ▼
Unique Jobs
    │
    │ (Supabase insert)
    ▼
Database Storage
    │
    │ (Query & analysis)
    ▼
Results
```

## 🔐 Security & Anti-Detection

```
┌─────────────────────────────────────────────────┐
│         Anti-Detection Mechanisms               │
├─────────────────────────────────────────────────┤
│                                                 │
│  1. Proxy Rotation                              │
│     └─ Random proxy per request                 │
│                                                 │
│  2. User Agent Rotation                         │
│     └─ Random UA from fake_useragent            │
│                                                 │
│  3. Random Delays                               │
│     └─ 3-8 seconds between requests             │
│                                                 │
│  4. Realistic Headers                           │
│     └─ Accept, Accept-Language, DNT, etc.       │
│                                                 │
│  5. Retry Logic                                 │
│     └─ 3 attempts with exponential backoff      │
│                                                 │
│  6. Rate Limit Detection                        │
│     └─ HTTP 429 handling                        │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 📊 Database Schema

```
┌─────────────────────────────────────────┐
│         indeed_jobs Table               │
├─────────────────────────────────────────┤
│                                         │
│  id             UUID [PK]               │
│  title          TEXT                    │
│  company        TEXT                    │
│  location       TEXT                    │
│  salary         TEXT [NULLABLE]         │
│  summary        TEXT                    │
│  link           TEXT [UNIQUE]           │
│  posted_date    TEXT                    │
│  scraped_at     TIMESTAMP [DEFAULT NOW] │
│                                         │
└─────────────────────────────────────────┘
```

## 🔄 Request/Response Cycle

```
┌─────────────┐    1. Request          ┌─────────────┐
│             │───────────────────────▶│             │
│  Scraper    │                        │ Indeed.com  │
│             │◀───────────────────────│             │
└─────────────┘    2. HTML Response    └─────────────┘
      │
      │ 3. Parse HTML
      ▼
┌─────────────┐
│ BeautifulSoup│
└─────────────┘
      │
      │ 4. Extract Data
      ▼
┌─────────────┐    5. Insert Data      ┌─────────────┐
│  Job Data   │───────────────────────▶│  Supabase   │
└─────────────┘                        └─────────────┘
```

## 📈 Performance Metrics

```
Single Request Timeline
│
├─ 0.0s: Start request
├─ 0.5s: DNS lookup
├─ 1.0s: TCP handshake
├─ 1.5s: SSL handshake
├─ 2.0s: HTTP request sent
├─ 3.0s: HTML received
├─ 3.5s: Parse HTML
├─ 4.0s: Extract data
├─ 4.5s: Save to DB
├─ 5.0s: Request complete
│
└─ Then wait 3-8 seconds before next request

Expected Performance:
• Request: ~5 seconds
• Delay: ~5 seconds (average)
• Total per page: ~10 seconds
• Jobs per page: ~10-15
• Efficiency: ~1 job per second
```

## 🎯 Error Handling Flow

```
Request Failed?
  │
  ├─ Yes ─────────────────────┐
  │                           │
  │                           ▼
  │                    Attempt < 3?
  │                           │
  │                           ├─ Yes ─▶ Exponential Backoff ─▶ Retry
  │                           │
  │                           └─ No ──▶ Log Error ─▶ Skip Page
  │
  └─ No ─▶ Parse Response ─▶ Continue
```

## 🧩 Module Dependencies

```
indeed_scraper.py
  ├─ requests (HTTP)
  ├─ beautifulsoup4 (parsing)
  ├─ lxml (HTML parser)
  ├─ fake-useragent (UA rotation)
  ├─ python-dotenv (.env handling)
  └─ supabase (database)

run_scraper.py
  ├─ indeed_scraper
  └─ config

view_jobs.py
  ├─ supabase
  ├─ python-dotenv
  └─ json

test_setup.py
  ├─ os
  ├─ sys
  └─ all above modules (for testing)
```

---

**This architecture ensures:**
- ✅ Modularity and maintainability
- ✅ Robust error handling
- ✅ Anti-detection measures
- ✅ Scalability
- ✅ Easy configuration
- ✅ Comprehensive logging
